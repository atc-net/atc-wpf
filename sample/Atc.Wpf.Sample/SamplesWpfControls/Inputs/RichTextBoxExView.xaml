<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpfControls.Inputs.RichTextBoxExView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sample="clr-namespace:Atc.Wpf.Controls.Sample;assembly=Atc.Wpf.Controls.Sample"
    xmlns:textFormatters="clr-namespace:Atc.Wpf.Documents.TextFormatters;assembly=Atc.Wpf"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <atc:GridEx Columns="*,400">

        <ScrollViewer Grid.Column="0" Style="{StaticResource SampleScrollViewerStyle}">
            <atc:UniformSpacingPanel x:Name="UsageArea" Style="{StaticResource SampleUsageAreaStyle}">

                <!--  Interactive demo  -->
                <TabControl>

                    <TabItem Header=" XAML ">
                        <atc:RichTextBoxEx
                            Margin="5,10,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{DynamicResource AtcApps.Brushes.ThemeBackground}"
                            FontFamily="Courier New"
                            Foreground="{DynamicResource AtcApps.Brushes.Text}"
                            HorizontalScrollBarVisibility="Auto"
                            IsReadOnly="True"
                            Text="{Binding Path=DataContext.XamlCode, Mode=OneTime}"
                            TextFormatter="{x:Static textFormatters:XamlFormatter.Instance}"
                            VerticalScrollBarVisibility="Auto" />
                    </TabItem>

                    <TabItem Header=" C# ">
                        <atc:RichTextBoxEx
                            Margin="5,10,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{DynamicResource AtcApps.Brushes.ThemeBackground}"
                            FontFamily="Courier New"
                            Foreground="{DynamicResource AtcApps.Brushes.Text}"
                            HorizontalScrollBarVisibility="Auto"
                            IsReadOnly="True"
                            Text="{Binding Path=DataContext.CSharpCode, Mode=OneTime}"
                            TextFormatter="{x:Static textFormatters:CSharpFormatter.Instance}"
                            VerticalScrollBarVisibility="Auto" />
                    </TabItem>

                    <TabItem Header=" JSON ">
                        <atc:RichTextBoxEx
                            Margin="5,10,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{DynamicResource AtcApps.Brushes.ThemeBackground}"
                            FontFamily="Courier New"
                            Foreground="{DynamicResource AtcApps.Brushes.Text}"
                            HorizontalScrollBarVisibility="Auto"
                            IsReadOnly="True"
                            Text="{Binding Path=DataContext.JsonText, Mode=OneTime}"
                            TextFormatter="{x:Static textFormatters:JsonFormatter.Instance}"
                            VerticalScrollBarVisibility="Auto" />
                    </TabItem>

                </TabControl>

            </atc:UniformSpacingPanel>
        </ScrollViewer>

        <sample:SampleSidePanel Grid.Column="1" SourceObject="{Binding}" />

    </atc:GridEx>

</UserControl>