<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpfControls.Inputs.ThicknessBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sample="clr-namespace:Atc.Wpf.Controls.Sample;assembly=Atc.Wpf.Controls.Sample"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <atc:GridEx Columns="*,400">

        <ScrollViewer Grid.Column="0" Style="{StaticResource SampleScrollViewerStyle}">
            <atc:UniformSpacingPanel x:Name="UsageArea" Style="{StaticResource SampleUsageAreaStyle}">

                <!--  Interactive demo  -->
                <atc:ThicknessBox
                    Width="460"
                    HorizontalAlignment="Left"
                    HideUpDownButtons="{Binding HideUpDownButtons}"
                    Maximum="{Binding Maximum}"
                    Minimum="{Binding Minimum}" />

                <!--  Showcase: Initial Value  -->
                <atc:ThicknessBox Width="350" Value="5,10,15,20" />

                <!--  Showcase: Hide Up/Down Buttons  -->
                <atc:ThicknessBox
                    Width="350"
                    HideUpDownButtons="True"
                    Value="10,10,10,10" />

                <!--  Showcase: Min/Max Constraints  -->
                <atc:ThicknessBox
                    Width="350"
                    Maximum="100"
                    Minimum="0"
                    Value="25,25,25,25" />

                <!--  Showcase: Disabled  -->
                <atc:ThicknessBox
                    Width="350"
                    IsEnabled="False"
                    Value="5,5,5,5" />

                <!--  Showcase: Bound Value Display  -->
                <StackPanel>
                    <atc:ThicknessBox
                        x:Name="BoundBox"
                        Width="350"
                        Value="8,16,8,16" />
                    <TextBlock Margin="0,5,0,0">
                        <Run Text="Current Value: " />
                        <Run Text="{Binding ElementName=BoundBox, Path=Value}" />
                    </TextBlock>
                </StackPanel>

            </atc:UniformSpacingPanel>
        </ScrollViewer>

        <sample:SampleSidePanel Grid.Column="1" SourceObject="{Binding}" />

    </atc:GridEx>

</UserControl>