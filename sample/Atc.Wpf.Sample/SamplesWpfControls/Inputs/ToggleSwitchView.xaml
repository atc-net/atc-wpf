<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpfControls.Inputs.ToggleSwitchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sample="clr-namespace:Atc.Wpf.Controls.Sample;assembly=Atc.Wpf.Controls.Sample"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <atc:GridEx Columns="*,400">

        <ScrollViewer Grid.Column="0" Style="{StaticResource SampleScrollViewerStyle}">
            <atc:UniformSpacingPanel x:Name="UsageArea" Style="{StaticResource SampleUsageAreaStyle}">

                <!--  Interactive demo  -->
                <atc:ToggleSwitch
                    ContentDirection="{Binding ContentDirection}"
                    IsEnabled="{Binding IsEnabled}"
                    IsOn="{Binding IsOn}"
                    OffContent="{Binding OffContent}"
                    OnContent="{Binding OnContent}" />

                <!--  Showcase: Static Examples  -->
                <atc:UniformSpacingPanel
                    Margin="10"
                    Orientation="Vertical"
                    Spacing="10">
                    <atc:ToggleSwitch />
                    <atc:ToggleSwitch IsEnabled="False" />
                    <atc:ToggleSwitch ContentDirection="LeftToRight" />
                    <atc:ToggleSwitch Content="Hello" />
                    <atc:ToggleSwitch Content="Hello" ContentDirection="LeftToRight" />
                    <atc:ToggleSwitch OffContent="No" OnContent="Yes" />
                </atc:UniformSpacingPanel>

            </atc:UniformSpacingPanel>
        </ScrollViewer>

        <sample:SampleSidePanel Grid.Column="1" SourceObject="{Binding}" />

    </atc:GridEx>

</UserControl>