<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpfControls.Progressing.OverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Atc.Wpf.Sample.SamplesWpfControls.Progressing"
    xmlns:sample="clr-namespace:Atc.Wpf.Controls.Sample;assembly=Atc.Wpf.Controls.Sample"
    d:DataContext="{d:DesignInstance Type=local:OverlayDemoViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <atc:GridEx Columns="*,400">
        <ScrollViewer Grid.Column="0" Style="{StaticResource SampleScrollViewerStyle}">
            <atc:UniformSpacingPanel
                x:Name="UsageArea"
                Style="{StaticResource SampleUsageAreaStyle}">

                <TextBlock Width="700" TextWrapping="Wrap">
                    The Overlay control provides a general-purpose translucent dimming layer
                    over wrapped content. Unlike BusyOverlay (which provides a built-in progress bar
                    and "Please wait" UI), the Overlay is content-agnostic — you provide any content
                    to display on top of the dimming layer. Supports fade animations, auto-close on
                    click, and customizable opacity.
                </TextBlock>

                <atc:AutoGrid Columns="*" Rows="Auto,*">

                    <!--  Controls  -->
                    <atc:UniformSpacingPanel
                        Margin="0,0,0,10"
                        Orientation="Horizontal"
                        Spacing="20">
                        <Button
                            x:Name="BtnToggleBasic"
                            MinWidth="120"
                            Click="OnToggleBasicClick"
                            Content="Show Overlay" />
                    </atc:UniformSpacingPanel>

                    <!--  Overlay wrapping content  -->
                    <atc:Overlay x:Name="BasicOverlay" AutoClose="{Binding AutoClose}">
                        <atc:Overlay.OverlayContent>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <atc:LoadingIndicator
                                    Width="48"
                                    Height="48"
                                    HorizontalAlignment="Center"
                                    Foreground="White"
                                    IsActive="True"
                                    SpeedRatio="1" />
                                <TextBlock
                                    Margin="0,12,0,0"
                                    HorizontalAlignment="Center"
                                    FontSize="14"
                                    Foreground="White"
                                    Text="Loading..." />
                            </StackPanel>
                        </atc:Overlay.OverlayContent>

                        <!--  Wrapped content  -->
                        <StackPanel Margin="20">
                            <atc:LabelTextBox LabelText="First name" WatermarkText="Enter first name" />
                            <atc:LabelTextBox LabelText="Last name" WatermarkText="Enter last name" />
                            <atc:LabelIntegerBox
                                LabelText="Age"
                                Maximum="120"
                                Minimum="0"
                                WatermarkText="Enter age" />
                        </StackPanel>
                    </atc:Overlay>
                </atc:AutoGrid>

                <atc:AutoGrid Columns="*" Rows="Auto,*">

                    <!--  Controls  -->
                    <atc:UniformSpacingPanel
                        Margin="0,0,0,10"
                        Orientation="Horizontal"
                        Spacing="20">
                        <Button
                            x:Name="BtnToggleCustom"
                            MinWidth="120"
                            Click="OnToggleCustomClick"
                            Content="Show Overlay" />
                    </atc:UniformSpacingPanel>

                    <!--  Overlay with custom card content  -->
                    <atc:Overlay x:Name="CustomOverlay" OverlayOpacity="{Binding OverlayOpacity}">
                        <atc:Overlay.OverlayContent>
                            <Border
                                MinWidth="300"
                                Padding="24"
                                Background="{DynamicResource AtcApps.Brushes.ThemeBackground}"
                                BorderBrush="{DynamicResource AtcApps.Brushes.AccentBase}"
                                BorderThickness="1"
                                CornerRadius="6">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,8"
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AtcApps.Brushes.ThemeForeground}"
                                        Text="Confirmation" />
                                    <TextBlock
                                        Margin="0,0,0,16"
                                        Foreground="{DynamicResource AtcApps.Brushes.Text}"
                                        Text="Are you sure you want to proceed with this action?"
                                        TextWrapping="Wrap" />
                                    <atc:UniformSpacingPanel
                                        HorizontalAlignment="Right"
                                        Orientation="Horizontal"
                                        Spacing="8">
                                        <Button
                                            MinWidth="80"
                                            Click="OnDismissCustomClick"
                                            Content="Cancel" />
                                        <Button
                                            MinWidth="80"
                                            Click="OnDismissCustomClick"
                                            Content="Confirm" />
                                    </atc:UniformSpacingPanel>
                                </StackPanel>
                            </Border>
                        </atc:Overlay.OverlayContent>

                        <!--  Wrapped content  -->
                        <StackPanel Margin="20">
                            <atc:LabelTextBox LabelText="Server" WatermarkText="Enter server address" />
                            <atc:LabelIntegerBox
                                LabelText="Port"
                                Maximum="65535"
                                Minimum="1"
                                WatermarkText="Enter port" />
                            <atc:LabelTextBox LabelText="Database" WatermarkText="Enter database name" />
                        </StackPanel>
                    </atc:Overlay>
                </atc:AutoGrid>

            </atc:UniformSpacingPanel>
        </ScrollViewer>
        <sample:SampleSidePanel Grid.Column="1" SourceObject="{Binding}" />
    </atc:GridEx>

</UserControl>