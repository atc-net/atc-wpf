<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpfControls.Progressing.BusyOverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:atcValueConverters="https://github.com/atc-net/atc-wpf/tree/main/schemas/value-converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Atc.Wpf.Sample.SamplesWpfControls.Progressing"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sample="clr-namespace:Atc.Wpf.Controls.Sample;assembly=Atc.Wpf.Controls.Sample"
    x:Name="UcBusyOverlayView"
    d:DataContext="{d:DesignInstance Type=local:BusyOverlayDemoViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <atc:GridEx Columns="*,400">
        <ScrollViewer Grid.Column="0" Style="{StaticResource SampleScrollViewerStyle}">
            <atc:UniformSpacingPanel x:Name="UsageArea" Style="{StaticResource SampleUsageAreaStyle}">

                <atc:BusyOverlay DisplayAfter="{Binding DisplayAfterMs, Converter={x:Static atcValueConverters:IntegerToTimeSpanValueConverter.Instance}}" IsBusy="{Binding IsBusy}">
                    <StackPanel Margin="20">
                        <atc:LabelTextBox LabelText="First name" WatermarkText="Enter first name" />
                        <atc:LabelTextBox LabelText="Last name" WatermarkText="Enter last name" />
                        <atc:LabelIntegerBox
                            LabelText="Age"
                            Maximum="120"
                            Minimum="0"
                            WatermarkText="Enter age" />
                    </StackPanel>
                </atc:BusyOverlay>

                <atc:GridEx Rows="Auto,*">

                    <atc:UniformSpacingPanel
                        Grid.Row="0"
                        Margin="10"
                        Orientation="Horizontal"
                        Spacing="10">
                        <Button
                            x:Name="BtnStartJob"
                            MinWidth="120"
                            Click="OnStartJobClick"
                            Content="Start Job" />
                        <TextBlock
                            x:Name="TbJobStatus"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource AtcApps.Brushes.Text}"
                            Text="Idle" />
                    </atc:UniformSpacingPanel>

                    <atc:BusyOverlay
                        x:Name="JobBusyOverlay"
                        Grid.Row="1"
                        DisplayAfter="00:00:00.1">
                        <atc:BusyOverlay.BusyContentAfter>
                            <Button
                                MinWidth="80"
                                Margin="0,0,0,8"
                                HorizontalAlignment="Center"
                                Click="OnCancelJobClick"
                                Content="Cancel" />
                        </atc:BusyOverlay.BusyContentAfter>
                        <StackPanel Margin="20">
                            <atc:LabelTextBox LabelText="Server" WatermarkText="Enter server address" />
                            <atc:LabelIntegerBox
                                LabelText="Port"
                                Maximum="65535"
                                Minimum="1"
                                WatermarkText="Enter port" />
                            <atc:LabelTextBox LabelText="Database" WatermarkText="Enter database name" />
                        </StackPanel>
                    </atc:BusyOverlay>
                </atc:GridEx>

            </atc:UniformSpacingPanel>
        </ScrollViewer>
        <sample:SampleSidePanel Grid.Column="1" SourceObject="{Binding}" />
    </atc:GridEx>
</UserControl>