<UserControl
    x:Class="Atc.Wpf.Sample.SamplesWpf.ValueConverters.LogCategoryTypeToBrushValueConverterSampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Atc.Wpf.Sample.SamplesWpf.ValueConverters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:LogCategoryTypeToBrushValueConverterSampleViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <atc:GridEx Columns="*" Rows="Auto,*,Auto">

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="LogCategoryTypeToBrushValueConverter" />
            <TextBlock
                Margin="0,4,0,0"
                Foreground="{DynamicResource AtcApps.Brushes.Gray3}"
                Text="The rectangle uses the brush converter directly; the ColorPicker is bound TwoWay so picking a new color updates the LogCategoryType palette and the brush converter rebuilds its cache."
                TextWrapping="Wrap" />
        </StackPanel>

        <GroupBox
            Grid.Row="1"
            Padding="10"
            Header="Palette">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:LogCategoryTypePaletteEntry}">
                            <atc:GridEx
                                Margin="0,4"
                                Columns="180,80,200"
                                Rows="Auto">
                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    FontFamily="Consolas"
                                    Text="{Binding Category}" />

                                <Rectangle
                                    Grid.Column="1"
                                    Width="60"
                                    Height="24"
                                    VerticalAlignment="Center"
                                    Fill="{Binding Brush}"
                                    RadiusX="3"
                                    RadiusY="3"
                                    Stroke="{DynamicResource AtcApps.Brushes.Gray5}"
                                    StrokeThickness="1" />

                                <atc:ColorPicker
                                    Grid.Column="2"
                                    Margin="20,0,0,0"
                                    BrushValue="{Binding DataContext.Brush, RelativeSource={RelativeSource AncestorType={x:Type ContentPresenter}}, Mode=OneWay}"
                                    ColorValue="{Binding DataContext.Color, RelativeSource={RelativeSource AncestorType={x:Type ContentPresenter}}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </atc:GridEx>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <StackPanel
            Grid.Row="2"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                MinWidth="180"
                Command="{Binding RandomizeSecurityCommand}"
                Content="Randomize Security Color" />
            <Button
                MinWidth="120"
                Margin="10,0,0,0"
                Command="{Binding ResetPaletteCommand}"
                Content="Reset Palette" />
        </StackPanel>

    </atc:GridEx>

</UserControl>