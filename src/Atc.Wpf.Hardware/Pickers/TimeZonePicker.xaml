<UserControl
    x:Class="Atc.Wpf.Hardware.Pickers.TimeZonePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Atc.Wpf.Hardware.Pickers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
    d:DataContext="{d:DesignInstance Type=local:TimeZonePicker}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="DefaultItemTemplate" DataType="{x:Type sys:TimeZoneInfo}">
            <atc:GridEx Columns="Auto,8,*" VerticalAlignment="Center">
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontFamily="Consolas"
                    Foreground="{DynamicResource AtcApps.Brushes.Gray5}"
                    Text="{Binding BaseUtcOffset, StringFormat=hh\\:mm}" />
                <TextBlock
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Text="{Binding DisplayName}" />
            </atc:GridEx>
        </DataTemplate>
    </UserControl.Resources>

    <ComboBox
        x:Name="PartComboBox"
        atc:TextBoxHelper.Watermark="{Binding RelativeSource={RelativeSource AncestorType=local:TimeZonePicker}, Path=WatermarkText}"
        ItemTemplate="{Binding RelativeSource={RelativeSource AncestorType=local:TimeZonePicker}, Path=ResolvedItemTemplate}"
        ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:TimeZonePicker}, Path=TimeZones}"
        SelectedItem="{Binding RelativeSource={RelativeSource AncestorType=local:TimeZonePicker}, Path=Value, Mode=TwoWay}" />
</UserControl>